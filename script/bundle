ENV['GEM_HOME'] = File.join(Dir.pwd,'vendor/bundle')
ENV['GEM_PATH'] = File.join(Dir.pwd,'vendor/bundle')
ENV['BUNDLE_GEMFILE'] = File.join(Dir.pwd,'Jemfile')

require 'rubygems'
require 'bundler'
require 'bundler/cli'

Bundler::CLI.start
